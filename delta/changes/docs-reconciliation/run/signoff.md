C9 signed-off-by: claude 2026-08-28 - ran delta/bin/install into a scratch $HOME (see C3, which now automates the file-count/name part of this); ran `delta/bin/verify example-verify-exit-codes` and the full six-change regression sweep (example-verify-exit-codes, reproduction-first-checks, global-install-lazy-init, revert-runner-bootstrap, telemetry-report, terminal-presence), all exit 0; ran `delta/bin/generate-commands --target project --check` after every edit to delta/commands/*.md, confirming no drift. The webhook/psql check example and the DELTA_ROOT=/path/to/repo line are illustrative syntax with placeholder data, not claimed as runnable against this repo, and were left as such (matches delta/commands/propose.md's own identical example).
C10 signed-off-by: claude 2026-08-28 - read README.md start to finish at 199 lines. A newcomer reaches `delta/bin/install` by line 19, the five-command table by line 34, and a runnable `delta/bin/verify` example by line 63 - all within the first third. Everything past "Working with any CLI" is reference material (terminal internals, telemetry, deliberately-not-built), not needed to get started. No section restates another; the two Layout trees are the only place file locations are shown in full.

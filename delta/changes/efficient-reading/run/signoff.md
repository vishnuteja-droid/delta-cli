C2 signed-off-by: claude 2026-08-29 - real counts from this repository's own history, not estimates. The first explore of the "docs" area happened during the docs-reconciliation CR, before delta/truth/docs.md existed: README.md, AGENTS.md, delta/constitution.md, all five delta/commands/*.md, five delta/bin/* header comments, delta/adapters.yaml - 13 files. docs-reconciliation was then archived into delta/truth/docs.md (commit d21cf69) specifically to make a real second explore possible. Following the new instructions just now: `git log -1 -- delta/truth/docs.md` (one commit, d21cf69), `git log --oneline d21cf69..HEAD -- README.md AGENTS.md CHANGELOG.md delta/changes/docs-reconciliation/` (empty, nothing changed) - truth answers everything, second explore opens exactly delta/truth/docs.md, one file. 13 to 1.
C4 signed-off-by: claude 2026-08-29 - see delta/changes/efficient-reading/dogfood/. payments-truth-before.md claims dispatch "posts synchronously... and waits for the response". payments-src/papi/dispatch.py calls ledger_client.publish_async and returns immediately - fire and forget. Following the new explore.md instructions against this fixture produced payments-explore.md, which names the contradiction under its own "Stale truth" heading, states code wins, and does not silently correct truth without flagging it.
C6 signed-off-by: claude 2026-08-29 - same fixture and findings file as C4. payments-src/papi/dispatch.py imports ledger_client, commented as "otherservice's client library" - an external dependency with no local source in the fixture, standing in for a real service boundary. payments-explore.md's call chain records it as an edge (a dotted arrow to "ledger_client (otherservice)") and never describes what that library does internally, consistent with "recorded as an edge and not followed". payments-src/papi/dispatch_test.py exists in the fixture and is never mentioned anywhere in payments-explore.md - grep confirms: `grep -c dispatch_test payments-explore.md` returns 0.

# truth

The current understanding of this system. **Only `archive` writes here.**

Empty on a fresh clone, which is correct: with empty truth every item in a
spec is ADDED, and `propose` asks the scoping questions that an existing system
would otherwise answer. Greenfield is not a separate mode.

One file per area, named for the area. Present tense only — no changelog, no
"as of", no "previously". Git holds the history and holds it better.

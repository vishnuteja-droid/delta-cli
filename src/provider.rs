//! Stub (implemented in prompt 2). LLM provider abstraction: async
//! streaming completions, retry/backoff, and cancellation, with
//! OpenAI-compatible and Anthropic implementations configured via
//! `config.rs`.
